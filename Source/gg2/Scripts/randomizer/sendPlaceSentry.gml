/**
 * argument0: The sentry's ownerPlayer
 */
 
write_ubyte(global.serverSocket, SENTRY_PLACE);
/*write_ubyte(global.serverSocket, ds_list_find_index(global.players, argument0));
socket_send(global.serverSocket);*/
